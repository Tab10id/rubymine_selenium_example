source 'http://srv-vhod-serverspec.elins.internal:8081/repository/rubygems'

# Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
gem 'rails', '~> 5.2'

# Use sqlite3 as the database for Active Record
gem 'sqlite3', '~> 1.3'

group :test do
  gem 'capybara', '~> 2.18'
  gem 'rspec-rails', '~> 3.7'
  gem 'selenium-webdriver', '~> 3.11'
end
